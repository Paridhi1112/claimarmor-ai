# Pyrefly configuration - disable type checking for this project
# These errors are false positives caused by pyrefly analyzing code blocks
# in isolation without seeing the full file's import context.
[python]
# Disable pyrefly diagnostics for all files
ignore_missing_imports = true

[[files]]
path = "**/*.py"
ignore = true
